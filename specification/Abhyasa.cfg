\* TLC configuration for Abhyasa.tla — operational model.
\* Checks the six safety invariants and the deliver-or-report liveness property
\* over a small custody-machine instance with an adversarial channel.

CONSTANTS
    Obligations = {o1, o2, o3, o4}
    Admissible = {o1, o2, o3}
    MaxRetries = 4
    Deadline = 6

SPECIFICATION Spec

INVARIANT Invariants

PROPERTY DeliverOrReportLive
